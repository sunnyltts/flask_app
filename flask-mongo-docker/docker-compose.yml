version: '3'
services:
  web:
   build: .
   ports:
     - "5000:5000"
   depends_on:
     - redis
     - mongo
  redis:
    image: redis
  mongo:
    image: mongo
    volumes:
      - mongodb_data:/data/db
volumes:
  mongodb_data: